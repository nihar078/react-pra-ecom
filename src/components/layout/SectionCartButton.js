import { Button } from "react-bootstrap";

const SectionCartButton = () => {
    return (
        <div className="text-center mt-3">
            <Button variant="secondary">See the cart</Button>
        </div>

    );
}

export default SectionCartButton;